<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <style>
      body {
        background-color: #333333;
        color: white;
        font-family: Arial, sans-serif;
        text-align: center;
        margin: 0;
        padding: 0;
      }

      /* Cabeçalho */
      .header {
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
        background-color: #222222;
      }

      .header img {
        width: 100px;
        margin-right: 20px;
      }

      .header h1 {
        font-size: 2rem;
        color: #ffde59; /* Cor dourada para o título */
      }

      /* Título do Catálogo */
      h2 {
        font-size: 1.8rem;
        margin: 20px 0;
        color: #ffde59;
      }

      /* Cards dos discos */
      .disco-card-container {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 20px;
        padding: 20px;
      }

      /* Responsividade */
      @media (max-width: 768px) {
        .disco-card-container {
          flex-direction: column;
          align-items: center;
        }
      }
    </style>
    <title>CD - Catálogo</title>
  </head>
  <body>
    <!-- Cabeçalho -->
    <div class="header">
      <img src="/images/cepar.png" alt="Logo CEPAR DISCOS" class="img-fluid">
      <h1>BEM VINDO(A) AO CEPAR DISCOS</h1>
    </div>

    <!-- Título do Catálogo -->
    <h2>Garanta já os discos das suas músicas favoritas!!!</h2>

    <div class="container">
      <div class="row">
        <!-- Dropdown para Filtrar por Artista -->
        <div class="col-md-6 mb-3">
          <div class="dropdown">
            <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
              Filtrar por artista
            </button>
            <ul class="dropdown-menu">
              <% artistas.forEach(function(artista) { %>
                <li><a class="dropdown-item" id="drop1" href="#" data-value="<%= artista.artista_id %>"><%= artista.nome %></a></li>
              <% }) %>
            </ul>
          </div>
        </div>
    
        <!-- Dropdown para Filtrar por Banda -->
        <div class="col-md-6 mb-3">
          <div class="dropdown">
            <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
              Filtrar por Banda
            </button>
            <ul class="dropdown-menu">
              <% bandas.forEach(function(banda) { %>
                <li><a class="dropdown-item" id="drop2" href="#" data-value="<%= banda.banda_id %>"><%= banda.nome %></a></li>
              <% }) %>
            </ul>
          </div>
        </div>
      </div>
    </div>
    
    <script>
      // Dropdown para Artista
      document.querySelectorAll('#drop1').forEach(item => {
        item.addEventListener('click', function() {
          let artistaSelecionado = this.getAttribute('data-value');
          
          // Redireciona para a URL com o parâmetro "artista"
          window.location.href = `/?artista=${artistaSelecionado}`;
        });
      });
    
      // Dropdown para Banda
      document.querySelectorAll('#drop2').forEach(item => {
        item.addEventListener('click', function() {
          let bandaSelecionada = this.getAttribute('data-value');
          
          // Redireciona para a URL com o parâmetro "banda"
          window.location.href = `/?banda=${bandaSelecionada}`;
        });
      });
    </script>
    
    
    


    <!-- Cards dos discos -->
<div class="disco-card-container">
  <!-- Loop para cada disco -->
  <% for (let disco of discos) { %>
    <div class="card" style="width: 18rem;">
      <img src="<%= disco.imagem %>" class="card-img-top" alt="<%= disco.titulo %>">
      <div class="card-body">
        <h5 class="card-title"><%= disco.titulo %></h5>
        <p class="card-text">Confira esse disco incrível! Não perca a chance de adquiri-lo.</p>
        <a href="/disco/<%= disco.disco_id %>" class="btn btn-primary">Mais Detalhes</a>
      </div>
    </div>
  <% } %>
</div>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  </body>
</html>
